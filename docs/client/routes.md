```
/login
/join
/new
/:namespaceName
/:namespaceName/:recipeSlug
/:namespaceName/:recipeSlug
/mix/:recipeRevisionId - need to think about this. might but it in state. with the ability to edit recipes while mixing, it's possibly incorrect to highlight an out of date ID here.
/:namespaceName/inventory
/inventory
/batches
/ingredients
/ingredients/:slug
/vendor
/vendor/:vendorCode
```
